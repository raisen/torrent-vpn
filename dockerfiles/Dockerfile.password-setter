FROM alpine:latest

# Install dependencies at build time
RUN apk add --no-cache curl

# Copy the password setter script
COPY scripts/password-setter.sh /app/password-setter.sh
RUN chmod +x /app/password-setter.sh

ENTRYPOINT ["/app/password-setter.sh"]
